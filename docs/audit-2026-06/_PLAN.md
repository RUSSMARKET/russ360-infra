# Аудит Russ360 — реестр потоков (фаза 1: картирование дефектов)

Последнее обновление: 2026-06-09

Метод: единица работы = один бизнес-поток end-to-end сквозь сервисы. Каждый запуск — ровно один pending-поток.
Известные/принятые проблемы (audit 2026-05-18 + memory) повторно НЕ логируются.

Инвентарь собран из routes/controllers всех 4 бэкендов (rusaifin 19 секций, sklad inventory+admin, rusaicore 5-entity CRUD, rusaiauth auth/registration/recovery).

## Приоритизация

Приоритет = риск для измерений аудита (корректность / целостность данных / арх-дрейф) с поправкой на пост-cutover. Чем выше cross-service связность с Core-контрактами и чем больше known-fragile сигналов — тем выше.

### TIER 1 — cross-service контракты + активные прод-инциденты (макс. сигнал)

| # | flow slug | описание | сервисы | known-сигналы | статус |
|---|-----------|----------|---------|---------------|--------|
| 1 | hiring-onboarding | Оформление: StaffRegistrationController (22 эндпоинта), `setRegistrationRole` → Core Employee+Membership | rusaifin, rusaicore | orphan users регрессия (open), SeedCoreEmployees bug | **done** (F-0002…F-0006) |
| 2 | point-agent-binding | Проекты: PointController `addPointAgent`/`deletePointAgent` → Core OperationalLocationAssignment | rusaifin, rusaicore | deprecated-но-активен PointService, orphan WriteGateway, frozen-pivot | **done** (F-0007…F-0012); ⚠ attachAgent/detachAgent legacy-only УЖЕ переписаны на Core (known-issue закрыт) |
| 3 | project-support-membership | Проекты: ProjectController + `support/add`/`support/delete` → project_supports legacy + Core ProjectMembership | rusaifin, rusaicore | visibility orphans backfill, dual-write | **done** (F-0013…F-0019); F-0013 = P1 (владелец: одна роль на проект) |
| 4 | registration-auth | Регистрация+OIDC: rusaiauth RegistrationController → rusaifin `internal/registration/*` set-password | fintech, rusaiauth, rusaifin | callback white page, error contract, дубли registration | **done** (F-0020…F-0025) |
| 5 | login-otp | rusaiauth LoginController password / otp-request / otp-verify + bootstrap `/auth/me` | fintech, rusaiauth, rusaifin | SMS-OTP полевых не проверен (Track F хвост) | **done** (F-0026…F-0029) |
| 6 | password-recovery-sync | rusaifin RestorePassword + rusaiauth Recovery + bidirectional password sync | rusaifin, rusaiauth, rusaisklad_back | silent password break (sync), set-password DB::update bypass | **done** (F-0030…F-0033) |

### TIER 2 — sklad inventory: машины состояний + мутация балансов (data-integrity)

| # | flow slug | описание | сервисы | статус |
|---|-----------|----------|---------|--------|
| 7 | inventory-transfer | Передачи: store/accept/cancel/rollback/update/take-from-promoter/delete | rusaisklad_back | **done** (F-0001) |
| 8 | inventory-check | Инвентаризация: checks/lines/submit/approve/reject/apply-corrections | rusaisklad_back | **done** (F-0034…F-0039) |
| 9 | inventory-issue-writeoff | Выдача/списание + writeoff-requests + corrections → balances/movements | rusaisklad_back | **done** (F-0040…F-0041); issue/writeoff/approve соблюдают lock-паттерн F-0001 — чисто |
| 10 | inventory-receipt-warehouse | Приёмка (receipts) + warehouse allocate/receive → balances | rusaisklad_back | **done** (F-0042…F-0044); allocate/receive/accept соблюдают lock-паттерн — чисто |
| 11 | inventory-requests-approvals | Generic InventoryRequestController approve/reject/execute/cancel + correction-requests | rusaisklad_back | **done** (F-0045…F-0047); диспетч requestType типобезопасен, execute идемпотентен под lock — чисто |
| 12 | sklad-assignments | AssignmentController supervisor↔promoter (getPromoters/updatePromoters/unassigned) | rusaisklad_back | **done** (F-0048, F-0049) |
| 13 | transfer-documents | InventoryDocumentController: sender/receiver upload, manager approve/reject, акт-генерация | rusaisklad_back | **done** (F-0050, F-0051); cancel-race против doc-approve = дубль F-0001 (кросс-ссылка) |

### TIER 3 — authz / role-mapping / видимость (drift-риск)

| # | flow slug | описание | сервисы | known-сигналы | статус |
|---|-----------|----------|---------|---------------|--------|
| 14 | staff-visibility | StaffVisibilityScopeService dual-read (Core gateway + legacy SQL) | rusaifin, rusaicore | P0/P2 known (re-checked 05-18) | **done** (F-0052…F-0055) |
| 15 | staff-management | Сотрудники: StaffController create/update/block/off-on + agent-test period | rusaifin, rusaicore | getStaff N+1 perf-фикс | **done** (F-0056…F-0060) |
| 16 | role-pages-permissions | Системные: RoleController, pages, role_pages, permissions (rusaifin authz) | rusaifin | «нет доступных страниц» | **done** (F-0065…F-0067) |
| 17 | sklad-pages-roles | sklad PagesController + RoleController: Core membership роль → pages (agent не маппится) | rusaisklad_back, rusaicore | «нет доступных страниц» | **done** (F-0061…F-0064); F-0061 = P1 формализует known «agent не маппится» |
| 18 | project-switch | CurrentProjectController / sklad projects/current — current_project_external_id | rusaifin, rusaisklad_back, rusaicore | — | **done** (F-0068…F-0069) |
| 19 | user-profile-me | UserController getUser + `/auth/me` — role/membership проекция во фронт | rusaifin, rusaisklad_back | — | **done** (F-0070…F-0073) |

### TIER 4 — локальные домены (ниже cross-service риск; проверять выборочно)

| # | flow slug | описание | сервисы | статус |
|---|-----------|----------|---------|--------|
| 20 | product-consent | ConsentSessionController — публичный PII flow (sms/verify/profile/complete) | rusaifin, fintech | **done** (F-0074…F-0076) |
| 21 | products-catalog | ProductController + project/point product attach + system sync-products-to-points | rusaifin | **done** (F-0077…F-0081) |
| 22 | shift-planning | ShiftController + PlannedShiftController + PlansController (графики, frozen-pivot) | rusaifin, rusaicore | **done** (F-0082…F-0084) |
| 23 | results-reporting | Результаты работы сотрудников (PlansController getResults) | rusaifin | **done** (F-0101…F-0103) |
| 24 | rusaifin-inventory-agent | InventoryItem/Threshold/WeeklyInventory/SupplyRequest (товар агентов на точках) | rusaifin | **done** (F-0104, F-0105) |
| 25 | requests-cards-magnit | RequestController + ruchnik + magnit export (карты) | rusaifin | **done** (F-0106…F-0109) |
| 26 | motivation | Penalties / BringFriend / Compensation | rusaifin | **done** (F-0110…F-0112) |
| 27 | sklad-sku-catalog | SKU / SKUCategory / ProjectSku (sklad) | rusaisklad_back | **done** (F-0091) |
| 28 | icontact-sync | IContactSyncController — внешний CRM bridge (sync runs/tasks/mappings) | rusaisklad_back | **done** (F-0092…F-0094) |
| 29 | sklad-projects-admin | sklad ProjectController + admin users/projects (shadow-sync off) | rusaisklad_back, rusaicore | **done** (F-0085…F-0087) |
| 30 | clients | ClientController | rusaifin | **done** (чисто — owner-скоуп везде) |
| 31 | tasks | TaskController | rusaifin | **done** (F-0113, F-0114) |
| 32 | chats | ChatController (+ единственный Reverb broadcast) | rusaifin | **done** (F-0095, F-0096) ⚠ P0 |
| 33 | education | EducationController | rusaifin | **done** (F-0115) |
| 34 | notifications-redirect | NotificationController + RedirectController (короткие ссылки) | rusaifin | **done** (F-0097, F-0098) |
| 35 | webhooks | WebhooksController | rusaifin | **done** (F-0099, F-0100) |
| 36 | core-crud | rusaicore 5-entity CRUD + EnsureIdempotency middleware (канонический writer) | rusaicore | **done** (F-0088…F-0090) |

## Журнал

- 2026-06-09: инвентарь построен из routes/controllers. Поток #7 inventory-transfer проаудирован → F-0001 (P1). 
- 2026-06-09: реестр расширен до 36 потоков (полный обход API всех бэкендов), приоритизация по 4 тирам.
- 2026-06-09: **TIER 1 закрыт полностью** (потоки 1–6). 33 находки (F-0001…F-0033): 0 P0, 9 P1, 14 P2, 7 P3 (вкл. F-0001 из inventory-transfer). Верификацией опровергнут 1 ложный P1 (rusaifin disabled-guard покрыт middleware). Открытые вопросы владельцу: F-0013 (мульти-ролинг на проекте — потенц. P0), F-0030 (снять ли legacy passwordrecovery роуты). Следующий тир: TIER 2 (sklad inventory state-machines, потоки 8–13).
- 2026-06-09: **Поток 8 (inventory-check) done** — F-0034…F-0039: 3 P1, 2 P2, 1 P3. Корневой паттерн = stale route-bound статус-guard без `lockForUpdate`+re-check внутри транзакции (класс F-0001) в `applyCorrections`/`approveCheck`/`rejectCheck`. Отдельно F-0036 (P1, confirmed): `recalculateExpectedAtSubmit` мутирует `qty_expected` in-place, `rejectCheck` не восстанавливает снапшот → детерминированный двойной счёт на reject→resubmit (субагент дал неверную корневую причину — исправлено при верификации). F-0035 (P1): TOCTOU инварианта `qty_total>=qty_reserved`.
- 2026-06-09: **Поток 10 (inventory-receipt-warehouse) done** — F-0042 (P1, confirmed): приёмка без идемпотентности → двойной клик удваивает складской `qty_total` (нет естественного потолка у RECEIPT, в отличие от issue/transfer); F-0043 (P2): `firstOrCreate`→`lockForUpdate` гонка на первом движении пары holder×sku → unique-violation 500 (системно: receipt/finalize-to-balance/balanceForUpdate); F-0044 (P3): роль получателя allocate не пере-проверяется на accept (TOCTOU membership). allocate/receive/accept сами по lock-паттерну чисты.
- 2026-06-09: **Поток 11 (inventory-requests-approvals) done** — F-0045 (P2, confirmed): approve/reject correction-request сверяют статус передачи по stale eager-load вне транзакции (только заявка под lock) → «approved-но-неисполнимый» хвост; F-0046 (P3): `correctionRequest()` singular `latestOfMany` после снятия UNIQUE скрывает историю заявок в очереди/ресурсе; F-0047 (P3, drift): write-экшены дженерик-контроллера без `reviewInventoryRequests`-gate (прикрыто сервисами, дыры нет). Диспетчеризация `requestType` типобезопасна (enum + match/default Forbidden), `execute` идемпотентен под lock через `executed_at` — подтверждено. Дропнута perf-находка субагента (in-memory pagination, вне scope).
- 2026-06-09: **Поток 12 (sklad-assignments) done** — F-0048 (P2, confirmed): `updatePromoters` тихо пропускает существующий ассигнмент (`exists`-skip) и строит ответ из входного списка → silent partial-success + нетранзакционный pre-check (TOCTOU); F-0049 (P2, likely): перепривязка промоутера во время активной инвентаризации рассинхронит снапшот/`take-from-promoter` (НЕ порча балансов — корректировки holder-keyed; субагент завысил до P1, снижено при верификации). Известные (orphan при удалении promoter, нет soft-delete, нет Core-интеграции) не дублированы.
- 2026-06-09: **Поток 13 (transfer-documents) done** — F-0050 (P2, confirmed): `file->store()` внутри DB-транзакции → orphan-файлы на публичном диске при откате; F-0051 (P3, drift): sender-upload не принимает alternate file-keys, в отличие от receiver. Находка субагента «cancel-transfer-no-lock races doc-approve-finalize» = ДУБЛЬ F-0001 (тот же `cancelTransfer` без lock, новый лишь триггер) → не залогирована, добавлена кросс-ссылка в F-0001. approve→finalize по документу построен корректно (lock+re-check), destroyForTransfer согласован.
- 2026-06-09: **Поток 14 (staff-visibility) done** — F-0052 (P1, confirmed): RG (REGIONAL_DIRECTOR) и PM (PROJECT_MANAGER) в дереве персонала проекта видят ПУСТЫЕ списки агентов — `visibleAgentUserIds` строится из Core-memberships (PM/RD/support), а агенты живут в assignments; carve-out `null` сделан только для SUPPORT_MANAGER. Верифицировано: isPrivilegedViewer не включает RG/PM, computeVisibleUserIds:151 только membershipProvider. F-0053 (P2): `hasAccessToUser` null-deref `[0]` → 500 (fail-closed) на denial-пути addReport. F-0054 (P2): товарная матрица RG/PM только из `[0]` проекта. F-0055 (P2, needs-verification): `core_employee_external_id` без UNIQUE → `pluck` схлопывает дубли в резолве видимости.
- 2026-06-09: **Поток 15 (staff-management) done** — F-0056/F-0057 (P2): block в rusaifin не синхронит Core Employee status и не делает identity-suspend (заблокированный может fresh-login по SSO) — write-side к reader-side F-0026/F-0027, смягчено detach'ем memberships (поэтому P2, не P1). F-0058 (P2): agent-test grant — Core HTTP-write внутри DB-транзакции без компенсации (класс F-0022). F-0059 (P2): смена роли в updateStaff завершает Core-привязки, но не reprovision'ит под новую роль. F-0060 (P2): createStaff без транзакции + непроектные роли молча без Core Employee (orphan, класс F-0002, другой entry point). Дропнут F6 субагента (duplicate-'who' History = дубль F-0003); 2 block-находки снижены P1→P2 (detach смягчает). Чисто: setUserPassword identity-sync, AgentTestPeriod state-machine/authz.
- 2026-06-09: **TIER 2 закрыт полностью** (потоки 7–13). 18 находок в TIER 2 (F-0001 + F-0034…F-0051; F-0001 проаудирован ранее): 0 P0, 4 P1 (F-0001, F-0034, F-0035, F-0036, F-0042 — итого 5 в sklad, из них 4 новых в TIER 2-окне), много P2/P3. Доминирующий паттерн sklad-домена: **проверка статуса state-machine на stale route-bound модели без `lockForUpdate`+re-check внутри транзакции** (класс F-0001) — повторяется в check (apply/approve/reject), correction-approve, cancel. Issue/writeoff/warehouse-accept/generic-execute — наоборот, паттерн соблюдают (чисто). Отдельный класс: отсутствие идемпотентности на sklad-write-роутах (F-0042 receipt double-credit). Верификацией: 1 находку реклассифицировал (F-0036 корневая причина), 1 снизил severity P1→P2 (F-0049), 2 находки субагентов дропнул (спекулятивный future-endpoint; perf-pagination), 1 = дубль F-0001. Следующий тир: TIER 3 (authz/role-mapping/видимость, потоки 14–19).
- 2026-06-09: **Поток 17 (sklad-pages-roles) done** — F-0061 (P1, confirmed): корень прод-инцидента «нет доступных страниц» — НЕТ mapping-слоя Core `project_role` (11 значений) → sklad `roles.code` (4 значения) в read-path. `CurrentProjectService:150,160` отдаёт сырую Core-роль в `Role::where('code')->first()` → null для `agent` + ~6 management-ролей → `UserContext::getPages:92-94` пустой `collect()`. Формализует/расширяет known «agent не маппится» (memory `no_available_pages_diagnosis`): не только agent. Замаскировано BAT-сидом (хардкод-нормализация в 4 кода). Верифицировано по коду end-to-end. F-0062 (P2): `getEffectiveRole` без fallback + мёртвая fallback-ветка `UserContext::resolveRole` (UserContextFactory всегда `hasExplicitRole=true`). F-0063 (P2): global-vs-membership admin-shortcut (`CurrentProjectService:146,169` global vs membership pages) — конкретизирует known 2026-05-18:141. F-0064 (P3): `/roles` отдаёт 4 кода (часть корня F-0061). Дропнут субагентский «stale-current-project-wrong-pages» (сам субагент признал by-design, нет конкретного сценария отказа).
- 2026-06-09: **TIER 3 закрыт полностью** (потоки 14–19). В окне 16–19: 13 находок (F-0061…F-0073): 1 P1 (F-0061), 6 P2, 6 P3. Доминирующий паттерн TIER 3: **множественность несогласованных резолверов роли в sklad** (Core membership `getEffectiveRole` для authz/pages vs `roleCodeForUser` global-first для /me vs legacy `getRoleCode` по локальному pivot) + расхождение словарей ролей Core(11)↔sklad(4). Верификацией реклассифицировано: F18-01 субагента P1→P2 (F-0068: per-request роль свежая → stale-кеш данных не утекает, только bootstrap-гейт); F-19-01 P1→P2 (F-0070: ограничено флагом project_selection_required); F-19-02 P1→P2 (F-0071: display-drift через UserResource, не authz happy-path); F-19-03 P2→P3 (F-0072: rusaifin authz глобальный, top-level авторитетна). Консолидировано: F18-01+F18-02+F18-03+F-19-04 → один F-0068 (общий кеш-корень). Дропнут субагентский «stale-current-project by-design» (поток 18). Кросс-ссылки: F-0072→F-0063, F-0073→F-0026/F-0027.
- 2026-06-09: **Поток 16 (role-pages-permissions, rusaifin) done** — F-0065 (P2, confirmed): `pages`/`role_pages` НИЧЕМ не сидятся (DatabaseSeeder зовёт только Magnit; миграция без data-insert) → чистая БД = пустая навигация всех ролей; прод замаскирован ручными данными → DR/CI/dev-риск (снижено P1→P2 при верификации: прод не сломан). F-0066 (P3): orphan `role_id` не guard'ится — `getPages` 500 (`->pages()` on null) / `getUserPermissions` тихий пустой 200; редко (deleteRole с закомментированным delete). F-0067 (P3): admin-bypass в permissions захардкожен, в `getPages` — нет (data-driven), при пустом role_pages админ без меню но с API. Чисто: role_pages composite-PK (нет дублей), CheckPermission middleware, межролевая утечка через `/api/pages?role_id=` (только admin-ветка), setRolePermission. Арх-дрейф authz-специфичный сверх известного — НЕ найден (roles/pages намеренно локальны, Core не владеет).
- 2026-06-09: **TIER 4 старт. Поток 20 (product-consent) done** — F-0074 (P1, confirmed): `resolvePhone` обнуляет `sms_send_attempts` (ProductConsentService:100) → единственный per-session лимит SMS снят, телефон из тела, throttle нигде нет (bootstrap/app.php api-группа без throttle, routes без throttle) → неограниченный SMS-bombing на произвольный номер. Новый аспект сверх known «нет rate limiting» = сам сброс defeats лимитер. F-0075 (P2): `prefill` отдаёт полный `toPrefillArray` (паспорт) только по `sms_verified_at`-гейту, `complete` его не сбрасывает → PII-oracle на весь 2ч TTL после завершения flow (токен сильный → не P1). F-0076 (P2): `complete` без транзакции/lock + нет UNIQUE на `customer_profile_consents.session_id` → гонка двойного complete плодит дубли юр.согласий. ЧИСТО: broadcast-канал маскирует phone (нет PII-утечки соседям), brute-force кода (6 цифр+лимит 10), token enum (Str::random(80)), phone-substitution (resolvePhone сбрасывает verify атомарно), authed bind/agentShow (IDOR нет). Известный общий тезис audit-05-18 не дублирован — даны конкретные эксплуатируемые file:line.
- 2026-06-09: **Поток 21 (products-catalog) done** — F-0077 (P1, confirmed): `SystemController::addToAllAgentsAllProducts:403` делает `detach($productIds)` вместо attach (комментарий и имя метода врут), ответ «успешно прикреплены» → один admin-GET стирает все `user_products` у ролей [3,10]. F-0078 (P2, новый инстанс F-0010): bulk add/deleteProductPointAgents — фейк-счётчики `attached=target_pairs`/`detached=target_pairs` + нетранзакционный Core-loop без try/catch (PointController:746-757,836-872); снижен P1→P2 — idempotent addAgent self-heal'ит при retry, дефект = инаккуратность отчёта/аудита. F-0079 (P3): deleteProduct — закомментированный delete (ProductController:328), но `status:true` + ложная History «Удаление». F-0080 (P2, likely): нет каскада — deleteProductCategories оставляет висячий `products.category_id`; deleteProjectProduct detach'ит только `projects_products`, точки сохраняют продукт в `point_products`. F-0081 (P3): system attach/detach product-to-point без транзакции (SystemController:168-171,260-263) vs sync обёрнут (309-314). ДРЕЙФА в продуктовом потоке НЕ найдено — addAgent/deleteAgent корректно пишут Core (D4/D5), legacy pivot не пишется; продукты — локальный домен rusaifin, не Core-сущность. Реклассифицировано 2: F-0078 P1→P2, F-0079 P2→P3. Авторизация write-эндпоинтов чистая.
- 2026-06-09: **Поток 22 (shift-planning) done** — frozen-pivot reader-switch в shift/plan ОСНОВНОМ пути ВЫПОЛНЕН (ownShiftPoints/memberUserIdsForLocalPointIds/resolveVisibleUserIds читают Core — верифицировано), новых «голых» frozen-readers, ломающих happy-path, нет. Находки: F-0082 (P2, confirmed, реклассиф. P1→P2): planned-shifts write (batchStore/update/batchDestroy) без object-level scope-check — любой staff.management мутирует смены чужих агентов; `authorize()=true` с `// TODO: Добавить проверку прав`; снижено т.к. актор — доверенный staff, нет privilege escalation (кандидат на повышение в фазе 2); консолидирует субагентские F-22-001+F-22-002. F-0083 (P2, likely): `shift/start` read-then-create без lock/unique → дубли открытых смен (класс F-0001). F-0084 (P3, likely): `User::point()` relation default-ветка всё ещё читает frozen `project_point_agents`/`group_leader_id`/`project_manager_id` (User.php:160-180, docblock признаёт намеренно) → `getStaffResult.is_transferred` неверен для post-cutover агентов; broader `whereHas('point')`-консьюмеры — sweep в фазе 2; кросс-ссылка F-0019/F-0071. ДРОПНУТ субагентский F-22-005 (getActivePoint GL=null — dead commented code, маргинально). Верифицировал по коду batchStore, route-middleware, authorize(), User::point.
- 2026-06-09: **Поток 29 (sklad-projects-admin) done** — ключевой вывод: проекты и memberships в sklad-admin прикрыты stage1-guard'ами (`disable_local_project/membership_writes` default true → 403), а **user-writes guard'а НЕ имеют** — дыра в guard-suite. F-0085 (P2, confirmed, реклассиф. P1→P2): `POST /admin/users` → `User::create` без Core-зеркала/back-sync → orphan, невидим Core-reads, непривязываем (membership закрыт); класс F-0002, вторичная legacy-точка входа sklad (не основной hire-flow rusaifin). F-0086 (P2, confirmed): `DELETE /admin/users` local-only → Core reverse-orphan + ре-материализация через `core:shadow-sync` («воскрешение»). F-0087 (P3, likely, реклассиф. P2→P3): `external_user_id` admin-editable без exists/unique-валидации против Core → silent mis-link; требует намеренного ввода. ЧИСТО (кросс-ссылки): project store/update/destroy guarded (default 403); reads через Core-контракты (= F-0068/F-0071, не дублирую); авторизация admin = ADMIN/MANAGER policy корректна.
- 2026-06-09: **⚠️ P0 ВСКРЫТ — Поток 32 (chats).** F-0095 (P0, confirmed): chat IDOR. `ChatService::__construct` пытается guard'ить через `if(!chatHasAccess()) return null;` — но возврат из конструктора PHP игнорирует → объект всегда валиден; контроллер `if($chat)` всегда true; мутирующие методы update/delete/getUsers/addUsers/deleteUsers НЕ перепроверяют доступ; роуты только `auth:oauth`+`UserIsNotDisabled`. Любой авторизованный юзер переименовывает/удаляет любой чат и читает PII участников (phone/email) перебором id. Верифицировано end-to-end по коду (ChatService:28-37,82,92,127-132,142,160; ChatController:172-176,219-223,273-279; routes 274-280). F-0096 (P3): addUsers без exists/dedup. ФЛАГ ВЛАДЕЛЬЦУ, НЕ чиню (протокол). Broadcast/Reverb в чатах НЕ используется (единственный канал — consent-session, authz корректна).
- 2026-06-09: **Поток 36 (core-crud, канонический writer) done** — writer построен добротно (слоистая арх., инварианты продублированы в БД: partial-unique + CHECK; FK restrict; external_id UNIQUE генерится сервером; «одна membership на (employee,project)» enforced и app-level и БД-level → чистый 409; все Action транзакционны). НО F-0088 (P1, confirmed): НЕТ scope-авторизации ни на одном write-роуте (routes:19,32-55 только auth:oauth; hasScope* нигде не вызывается) → любой токен с aud=core пишет всё, минуя бизнес-guard'ы. F-0089 (P2, confirmed): EnsureIdempotency read-then-write race → для membership/assignment спасает partial-unique БД (409), но CreateEmployee без естественного dedup → гонка плодит дубль employee (класс F-0042). F-0090 (P3): idempotency-ключ глобальный (не client-scoped) → реплей ответа между клиентами. ЧИСТО/кросс-ссылки: F-0055 (employee external_id) на Core-стороне UNIQUE — проблема в rusaifin-маппинге, не в Core. Дропнут F-CORE-04 (GET-обёртка idempotency — косметика, не дефект).
- 2026-06-09: **Поток 27 (sklad-sku-catalog) done** — F-0091 (P1, confirmed): `SKUService::deleteFromProject:113-119` удаляет только pivot `project_skus`, оставляя `inventory_balances`/`movements` на глобальный sku_id (FK балансов на `skus`, не на pivot → каскада нет) → фантомные остатки qty>0, невидимые в листинге; повторное добавление SKU «воскрешает» баланс. Bulk SKU→project отсутствует (F-0010 N/A). Дропнут F#27-2 (глобальный SKU не GC'тся — намеренно shared catalog, не дефект). Авторизация (manageSkus/viewSkus) чистая, сервисы транзакционны.
- 2026-06-09: **Поток 28 (icontact-sync, внешний CRM) done** — F-0092 (P1, likely): движение `issue` (своя tx) и обновление снапшота `last_applied_qty` НЕ атомарны; `issue` без idempotency-key → краш между ними + retry = двойное списание остатка. F-0093 (P2, needs-verification): снапшот keyed без `occurred_at`, apply-группа — с `occurred_at` → дубль-SKU-строки одной сессии применяются дважды (verify парсер). F-0094 (P2, confirmed): `process()` catch-all → FAILED вместо re-throw → job `tries=3` мёртв; SIGKILL mid-run → run застревает QUEUED/RUNNING → проект навсегда залочен от синков. Creds (encrypted+hidden) и `upsertMappings` (транзакц.+реальные счётчики) чисты. Известный `triggered_by` nullable (audit-05-18) не дублирован.
- 2026-06-09: **Поток 34 (notifications-redirect) done** — F-0097 (P2, confirmed): `redirect/history/{id}` (routes:502) БЕЗ middleware → утечка IP/UA/referrer перебором id (соседние CRUD под admin). F-0098 (P3, confirmed): open-redirect `redirect/to/{code}` — но redirect_to задаёт только админ (P3). NotificationController IDOR-устойчив (скоуп `where user_id`). 
- 2026-06-09: **Поток 35 (webhooks, внешний вход) done** — F-0099 (P1, confirmed): `webhook/<secret>/clear/?id=` (routes:549) БЕЗ auth удаляет ShiftReport любого юзера перебором id (+ null→500, забытый `->save()`, `$_GET` без валидации). F-0100 (P2, confirmed): все 3 webhook'а — секрет-в-URL (логируется), GET на мутациях, нет идемпотентности → дубли import/export. Класс: webhook-auth через статичный URL-секрет без HMAC.
- 2026-06-09: **Потоки 23-26, 30, 31, 33 (выборочные) done.** #23 results: F-0101 (P2, getAgentResult без reporting-scope — РГ читает чужих агентов; реклассиф. P1→P2, внутр.staff), F-0102 (P2, getStaffShiftById роут без CheckPermission — любой auth юзер читает чужие смены; реклассиф. P1→P2), F-0103 (P3, undefined date key→500). #24 inv-agent: F-0104 (P2, status state-machine без lock/transition-guard, класс F-0001), F-0105 (P3 nv, orphan заявок при удалении точки — FK не верифицирован). authz/транзакционность инвентаризации чисты. #25 ruchnik/magnit: F-0106 (P1, deleteRuchnik роут 372 без authz → деструктивный IDOR, верифицировано), F-0107 (P2, createRuchnik произвольный user_id), F-0108 (P2, magnit-app файлы на исходную заявку — copy-paste id), F-0109 (P3, setField race). #26 motivation (деньги): F-0110 (P1, Compensations модель→таблица с КИРИЛЛИЧЕСКОЙ `с` U+0441, миграция латинская — верифицировано hexdump; латентно на проде, ломает dev-refresh/новый стенд), F-0111 (P2, comment NOT NULL vs optional→500), F-0112 (P3, нет from_user_id аудита). authz штрафов/компенсаций (route-level *.management) чист. #30 clients: ЧИСТО (owner-скоуп везде). #31 tasks: F-0113 (P1, update/delete/comment роуты 295-302 без CheckPermission и без ownership → деструктивный IDOR любой задачи, верифицировано), F-0114 (P3, OR-precedence обход soft-delete — субагент ошибочно заявил cross-user leak, ОПРОВЕРГНУТО: все OR-ветки на свой id). #33 education: F-0115 (P2 likely, product-скоуп только в 1 из 3 read-методов → утечка метаданных материалов). РЕКЛАССИФ: F-RR-01/02 P1→P2, TASK-AUTHZ-ORWHERE P1→P3. ДРОПНУТЫ 2 маргинальных (TASK-SVC-NULL-GUARD — exists-валидация покрывает; EDU-DELETE-NPE — exists-валидирован). W-1 (webhook clear) = дубль F-0099, кросс-ссылка.
- 2026-06-09: **🏁 TIER 4 ЗАКРЫТ ПОЛНОСТЬЮ (потоки 20-36).** 42 находки в окне TIER 4 (F-0074…F-0115): **1 P0** (F-0095 chat IDOR — конструктор-guard `return null` не работает; ФЛАГ владельцу, не чинить), 7 P1 (F-0074 SMS-bombing, F-0077 detach-вместо-attach, F-0088 Core без scope-authz, F-0091 SKU detach orphan'ит балансы, F-0092 icontact non-atomic double-post, F-0099 webhook clear без auth, F-0106 deleteRuchnik IDOR, F-0110 compensation homoglyph, F-0113 task IDOR — итого с P0: 9 высоких в TIER 4), много P2/P3. ДОМИНИРУЮЩИЙ КЛАСС TIER 4: **broken object-level/route authz на write-роутах** (chats P0, tasks/ruchnik/staff-shift/planned-shifts P1-P2) — роуты под только `auth:oauth` без `CheckPermission` + сервисы без ownership-проверки; антипаттерн «конструктор-guard через `return null`» (chats=P0, tasks=безвреден из-за exists). Вторичные классы: нетранзакционный bulk + фейк-счётчики (F-0078, новый инстанс F-0010), отсутствие идемпотентности на write (Core employee F-0089, icontact F-0092, receipt-класс), homoglyph-баг таблицы (F-0110). Core canonical writer — целостность/state-machine защищены на уровне БД (хорошо), но scope-authz записи отсутствует (F-0088). Верификацией: реклассифицировано ~7 находок субагентов (5× P1→P2, 1× P2→P3, 1× P1→P3), опровергнут 1 cross-user-leak (F-0114), дропнуто ~5 маргинальных/дублей. **ВЕСЬ АУДИТ (потоки 1-36) ЗАКРЫТ: 115 находок — 1 P0, 24 P1, 58 P2, 32 P3.** Следующее: фаза 2 — решения владельца по находкам (приоритет: P0 F-0095 + 24 P1).
- 2026-06-09: **Поток 9 (inventory-issue-writeoff) done** — F-0040, F-0041 (оба P3). Поток оказался крепким: `issue`/`writeoff`/`approveRequest`/`rejectRequest`/`cancelRequest` соблюдают lock-паттерн F-0001 (`balanceForUpdate` + `lockForUpdate` заявки + re-check `status==PENDING` внутри транзакции). Дропнута спекулятивная находка субагента «issue-service-wider-than-endpoint» (нет текущего сценария отказа). F-0040: Postgres `unsignedBigInteger`=signed bigint, нет CHECK≥0 (latent). F-0041: pending writeoff-request держит резерв бессрочно.
